# X-04 订单与售后链路（成员 D 交付物）

> 周次：X4（Phase 4 · Day 3-4）｜ 日期：2026-08-15 ｜ 状态：✅ 完成
> 对应任务书：`docs/phase4/member-d/tasks/X1-X5-移动端开发任务书.md` §X4

## 一、本周目标

订单列表/详情 / 售后中心 / 消息通知跑通 U-013~U-025，完成 P0 履约链路与售后链路。

## 二、完成内容

### 1. 新增 API 模块

| 文件 | 契约 | 说明 |
|---|---|---|
| `src/api/refund.js` | U-018/U-019/U-020/U-021 | 退款发起、列表、撤销、申请介入 |
| `src/api/notice.js` | U-022/U-023/U-025 | 通知列表、单条已读、全部已读 |

### 2. 订单列表页（D-P05 `pages/order/list.vue`）

- U-013 列表：6 状态 Tab（全部/待支付/待发货/待收货/已完成/已取消，T1 状态值驱动）
- firstItemImage 首图展示 + 图片失败占位兜底
- 订单卡片：订单号/状态色/商品首图/件数/金额/时间
- 按钮随 T1 状态显隐：待支付→去支付/取消；待收货→确认收货；已完成未评价→评价
- 分页加载 + 空状态/未登录/加载失败 三态兜底
- 支持从「我的」页 URL 参数预选 Tab

### 3. 订单详情页（`pages/order/detail.vue`）

- U-014 详情：状态卡片（5 态图标+描述+背景色）、收货信息快照（receiverSnapshot 逗号分隔拆分）、运单号、商品明细快照（titleSnapshot/priceSnapshot×quantity/小计）、金额明细、订单信息（编号/下单/支付/发货/完成时间）
- 底部操作栏（T1 状态机驱动）：
  - PENDING_PAY：立即支付（U-015）+ 取消订单（U-016）
  - SHIPPED：确认收货（U-017）
  - COMPLETED 未评价：去评价（U-024）
  - PAID/SHIPPED/COMPLETED 无进行中售后：申请退款（U-018）
- 支付/取消/确认收货操作均带 loading + 二次确认 + 成功后自动刷新

### 4. 售后中心页（D-P08 `pages/refund/list.vue`）

- U-019 退款列表：6 态展示（T1 售后状态机 + 颜色映射）
- 商家回复（merchantReply）持久化展示
- 管理员裁决（adminResult）持久化展示
- 按钮随状态显隐：
  - REFUNDING：撤销退款（U-020，二次确认）
  - MERCHANT_REJECTED：申请平台介入（U-021，二次确认）
- 分页加载 + 空状态兜底

### 5. 退款申请页（`pages/refund/apply.vue`）

- U-018 发起退款：订单编号/退款金额（前端校验 >0）/退款原因（≤200 字）
- 错误处理：5002/5003/5004/5006 由请求层 toast + 表单内 error tips
- 提交 loading 防重复
- 成功后 toast → 跳「我的」页

### 6. 消息通知页（D-P06 `pages/notice/list.vue`）

- U-022 通知列表：筛选条（全部/未读/已读）+ 全部已读按钮
- 未读通知左侧红色指示条 + ● 标记
- 点击未读通知自动标已读 U-023
- U-025 全部已读：成功后列表更新 + toast
- 分页加载 + 空状态兜底

### 7. 评价页（`pages/order/review.vue`）

- U-024 评价：星级评分 1-5 ★/☆ 交互、评价内容 ≤200 字计数
- 提交 loading 防重复
- 成功后 toast → 返回上一页

### 8. 登录守卫扩展

- PROTECTED_ROUTES 追加 9 条新路由（`src/utils/auth.js`）

## 三、验证记录（2026-08-15）

### 1. 构建

- `npm run build:h5` 通过（uni 编译器 vue3）。

### 2. 新增页面清单（pages.json 注册）

| 页面 | 路由 | 说明 |
|---|---|---|
| 订单列表 | pages/order/list | D-P05，6 状态 Tab + 分页 |
| 订单详情 | pages/order/detail | U-014 全字段展示 + T1 操作栏 |
| 订单评价 | pages/order/review | U-024 1-5 星 + 文字评价 |
| 售后中心 | pages/refund/list | D-P08，6 态流转展示 |
| 退款申请 | pages/refund/apply | U-018 表单 + 校验 |
| 消息通知 | pages/notice/list | D-P06，已读/未读筛选 |
| 收货地址 | pages/mine/address | U-003~007，增删改查/设默认 |
| 编辑地址 | pages/mine/address-edit | U-004/U-005 表单 |
| 个人资料 | pages/mine/profile | U-001/U-002 查看/修改 |

### 3. browser-use 实测（dev:h5 + XHR mock 注入）

无后端环境下注入 window.XMLHttpRequest mock，逐场景验证：

| 场景 | 验证点 | 结果 |
|---|---|---|
| 订单列表（全部） | 6 Tab 切换、订单卡渲染、金额/时间/状态色 | ✅ |
| 订单列表（待支付） | 「去支付」「取消」按钮显示、点击弹确认 | ✅ |
| 订单列表（待收货） | 「确认收货」按钮显示 | ✅ |
| 订单详情 | 状态卡 5 态、收货信息拆分、商品明细快照、金额汇总、订单信息 | ✅ |
| 支付操作 | PENDING_PAY→PAID 状态刷新 | ✅ |
| 取消订单 | 二次确认 → CANCELLED 状态刷新 | ✅ |
| 确认收货 | 二次确认 → COMPLETED 状态刷新 | ✅ |
| 售后列表 | 6 态展示 + 商家回复/平台裁决展示 | ✅ |
| 撤销退款 | REFUNDING → CLOSED | ✅ |
| 申请介入 | MERCHANT_REJECTED → ADMIN_INTERVENED | ✅ |
| 退款申请 | 表单校验（空金额/空原因拦截）+ 提交成功跳转 | ✅ |
| 消息通知 | 未读/已读筛选 + 点单条已读 + 全部已读 | ✅ |
| 评价 | 1-5 星评分 + 文字 + 提交成功返回 | ✅ |
| console | 全程无 JS error | ✅ |

## 四、遗留（依赖 M3）

- 真实数据端到端（订单/售后/通知来自 MySQL）：待 A 的 MySQL 8 环境就绪后按 mock 场景复测。
- 小程序端 `build:mp-weixin` 与 App 端验证：X5 端适配任务统一执行。

## 五、验收对照（任务书 §X4）

- [x] 订单全状态按钮显隐与 T1 完全一致（PENDING_PAY 支付+取消 / SHIPPED 确认收货 / COMPLETED 评价 / 可退款 退款入口）
- [x] 售后 6 态流转闭环（REFUNDING→商家处理→介入→裁决）展示正确
- [x] 商家回复与管理员裁决持久化可见
- [x] 消息已读/全部已读生效
- [x] `npm run build:h5` 通过