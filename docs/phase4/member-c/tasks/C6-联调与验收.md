# C6 联调与验收

> 成员 C ｜ 计划 Day 5 ｜ 状态：✅ 已完成（2026-08-29，M3 联调完成）
> 关联 skill：`.qoder/members/member-c/skills/backend-contract-check.md`
> 关联 mcp：`.qoder/members/member-c/mcps/browser-use-guide.md`

## 目标

与 A 后端完成联调（里程碑 M3/M4 的后台侧闭环），补齐空状态与体验细节，归档联调记录、演示说明与 AI 交互过程，同步进度文件并提交推送。

## 输入依赖

- C3/C4/C5 全部完成
- A 的 MySQL 8 环境就绪 + `backend/sql/` 建库种子脚本
- D 的演示路径（固定账号/商品/订单/退款单，D-02 方案）
- 作业要求实验报告 7 部分（交互过程 30 分 / 测试 20 分的记录素材）

## 工作步骤

1. **环境准备**：与 A 确认后端可联调（MySQL 起库 → `002_seed.sql` 导入 → 后端起服）；前端 `npm run dev` 通过代理对接。
2. **契约逐项核对**：用 backend-contract-check 将 34 个接口（M-001~015 / A-001~019）与页面调用逐项核对，输出核对表，发现问题走 backlog 记录并反馈 A。
3. **空状态与体验细节**（P1 验收项 + 作业要求"体验细节优化"）：
   - 无数据列表显示空状态插画 + 引导文案（不空白、不报错）
   - 加载态、操作后即时刷新、成功/失败反馈（ElMessage）
   - 长时间未审核的售后/入驻单据给出状态提示（如"已等待 N 天，可申请平台介入"入口与条件对应状态机）
4. **三方联动演示**：按 D 的演示路径完整走一遍，录制/截图留档；输出 `deliverables/C6-演示脚本与截图说明.md`。
5. **AI 交互记录归档**：按 D 的交互记录模板（每轮提问、AI 回复、思考判断），归档到 `deliverables/C6-AI交互记录.md`（对应作业"交互过程 30 分"）。
6. **进度同步**：更新进度文件四件套（PROJECT_STATUS / member-status / roadmap / backlog）+ 本看板状态，git add + commit + push 到 develop。

## 产出物

- 文件：`deliverables/C6-契约核对表.md`、`C6-演示脚本与截图说明.md`、`C6-AI交互记录.md`
- 内容：联调记录 + 演示脚本 + 实验报告素材

## 验收标准

- [x] 34 个接口页面调用全部核对通过（33 有调用 + A-013 标注暂不调用；3 条模糊点已记录待 M3 闭环）
- [x] 空状态/加载态/操作反馈全部补齐（13 页 v-loading + el-empty + 分页 + ElMessage 统一反馈）
- [x] 三方联动演示全程无报错，与 D 演示路径一致（**M3 完成**：2026-08-29 H2 环境四条链路实测流转通过，演示脚本可执行）
- [x] AI 交互记录覆盖需求细化/设计/实现/测试各阶段（C1-C6 六阶段 + 5 个关键判断轮次）
- [x] 进度文件四件套已更新并提交推送 develop

## 状态

- [ ] 未开始　[ ] 进行中　[x] 已完成

## 完成记录（2026-08-15）

- **契约汇总**：`C6-契约核对表.md`——34 接口全量核对（M 组 15/15、A 组 18/19 + P-002/P-003），分页/金额/枚举/必填/二次确认/审计留痕 6 类核对项全通过；3 条模糊点（M-002 张力、图片上传缺失、A-013 无归属）记录并标注反馈路径
- **空状态核查**：13 页全部具备 v-loading 加载态、el-empty 空状态插画 + 引导文案、分页控件、ElMessage 操作反馈
- **演示脚本**：`C6-演示脚本与截图说明.md`——5 大场景 23 个截图位（入驻审核闭环/上架巡检/发货/退款深度分支/权限隔离），待 M3 环境执行
- **AI 交互归档**：`C6-AI交互记录.md`——C1-C6 六阶段交互总览 + 5 个关键判断轮次（错误码纠偏/契约张力/A-013 归属/越权实测方案/并行冲突防护）+ 测试验证记录（build 2 次 + browser-use 3 轮 + 静态核查 2 轮）
- **遗留移交**：真实联调依赖 A 的 MySQL 8 环境（里程碑 M3），待 M3 达成后执行演示脚本与"待 M3 验证清单"（5 项），相关事项已同步 backlog

## M3 真实联调记录（2026-08-29，H2 MySQL 兼容模式）

- **环境**：`java -jar target/jd-shop-1.0.0.jar --spring.profiles.active=h2`（后端 8080，H2 内嵌库 schema+seed 自动执行）
- **登录**：merchant001 / admin001 / merchant002 密码登录成功，JWT 签发正常（role/shopId claims 正确）
- **错误码实测**：无 token → 1002、伪造 token → 1002、merchant 访问 /api/admin/** → 1003、admin 访问 /api/merchant/** → 1003；后端 HTTP 恒 200 + body.code，与前端 request.js 拦截处理完全一致
- **接口连通**：13 个核心接口全部 200（M-001/004/009/010/012/015 + A-001/004/008/010/014/016/018/019），分页 {list,total} 结构符合契约
- **四条链路流转**：入驻驳回 → 上架通过 → 巡检下架 → 发货 → 商家拒绝退款 → 管理员裁决，全程真实落库；审计日志 6 条（A-018）全部留痕可见
- **差异闭环**：模糊点 1 实测——M-002 对 REJECTED 返回 6005、A-002 对 REJECTED 返回 7005，后端无"REJECTED→PENDING_AUDIT 重提"接口（T1 3.2 缺失）；新增差异——M-006 仅允许 DRAFT 提交，OFF_SALE 返回 3005（T1 4.2 缺失）。均已反馈 A（见 backlog）
- **验收结论**：C6 全部验收标准达成；前端 admin-web 无需改动即可对接真实后端（vite 代理 /api → 8080）
