# C5 交互验收清单

> 成员 C ｜ 2026-08-15 ｜ 验证方式：browser-use 实测（dev server 5174）+ 代码静态核查
> 结论：**权限隔离与越权拦截实测通过；状态按钮显隐静态核查 13 页全通过；问题清单 0 项遗留**

## 1. 权限隔离与越权拦截实测（browser-use）

| # | 用例 | 操作 | 预期 | 实测结果 |
|---|---|---|---|---|
| 1 | 未登录重定向 | 无登录态打开 http://localhost:5174/ | 跳转 /login | ✅ 重定向 /login |
| 2 | 商家菜单隔离 | 注入 merchant001（role=MERCHANT）后打开首页 | 仅 6 个商家菜单，无管理员菜单 | ✅ 菜单为：店铺仪表盘/商品管理/库存管理/订单处理/售后处理/店铺设置，进入 /merchant/dashboard |
| 3 | 商家越权访问管理员页 | 商家态直接导航 /admin/users | 守卫警告 + 回退商家首页 | ✅ 最终停留 /merchant/dashboard |
| 4 | 管理员菜单隔离 | 注入 admin001（role=ADMIN）后打开首页 | 仅 7 个管理员菜单，无商家菜单 | ✅ 菜单为：审核中心/类目管理/商品巡检/商家管理/用户管理/争议处理/日志统计，进入 /admin/audit |
| 5 | 管理员越权访问商家页 | 管理员态直接导航 /merchant/dashboard | 守卫警告 + 回退管理员首页 | ✅ 最终停留 /admin/audit |
| 6 | 审核中心渲染 | 管理员态打开 /admin/audit | 双 tab（商家入驻审核/商品上架审核）+ 状态筛选 tab + 空状态 | ✅ 全部渲染，空状态「暂无商家数据」 |

> 补充说明：用例 2-5 中守卫警告文案「无权限访问该模块」对应 C-02 第 3 节 1003 语义；后端拦截由 T4 拦截矩阵保证（1002/1003 返回已在 request.js 映射提示）。

## 2. 状态按钮显隐核查（代码静态核查，明细见 C5-状态按钮映射表.md）

| 范围 | 核查内容 | 结果 |
|---|---|---|
| 商家 6 页 | 订单仅 PAID 发货；商品 4 态按钮；售后仅 REFUNDING 同意/拒绝；店铺 3 态按钮 | ✅ 全部与 T1 合法流转一致 |
| 管理员 7 页 | 入驻仅 PENDING_AUDIT 审核；上架仅 PENDING_ON_SALE 审核；巡检仅 ON_SALE 强制下架；争议仅 ADMIN_INTERVENED 裁决；用户 NORMAL/DISABLED 双向 | ✅ 全部与 T1 合法流转一致 |
| 禁止流转清单（T1 1.3/2.3/3.3/4.3） | 10 项禁止流转逐条核查可能入口页面 | ✅ 无禁止流转入口出现在任何页面 |
| 必填校验 | 驳回 auditReason（A-003/A-009/A-012）、裁决 adminResult+agree（A-015）、reply（M-013/M-014）、shippingNo（M-011） | ✅ 全部必填校验 + 敏感操作二次确认 |
| 越权提示文案 | CODE_MESSAGES 覆盖 1002/1003/6001-6005/7001/7002/7003/7005 等 25 码，与 T3 原文一致 | ✅ request.js 集中映射 |

## 3. 三方协作链路演练（种子数据静态推演）

> 真实链路联调待 M3（MySQL 8 环境未部署），当前按 seed.sql 演示数据 + T1 流转表推演各角色操作入口，前端页面已全部就绪：

| 链路 | 用户端 | 商家端（本端） | 管理员端（本端） | 前端就绪 |
|---|---|---|---|---|
| 1 商家入驻 | 提交入驻（U 组） | 店铺设置页 PENDING_AUDIT 横幅「审核中」+ REJECTED 修改重提 | 审核中心：PENDING_AUDIT 通过/驳回（驳回原因必填留痕） | ✅ |
| 2 商品上架 | 浏览 ON_SALE 商品（P-003~006） | DRAFT 提交上架 → PENDING_ON_SALE 只读等待 → OFF_SALE 重新提交 | 上架审核：通过（→ON_SALE）/驳回（→DRAFT）；巡检强制下架（原因必填留痕） | ✅ |
| 3 履约 | 支付 PAID → 确认收货（U 组） | 订单处理：仅 PAID 发货（运单号必填） | （无订单操作入口，A-013 暂不调用） | ✅ |
| 4 退款 | 申请退款 → 商家拒绝后申请介入（U 组） | 售后处理：仅 REFUNDING 同意/拒绝（回复必填）；介入后只读 | 争议处理：仅 ADMIN_INTERVENED 裁决（意见+结论必填，二次确认） | ✅ |

深度分支核对：`REFUNDING → 商家拒绝 → MERCHANT_REJECTED → 用户申请介入 → ADMIN_INTERVENED → 管理员裁决 → REFUNDED/CLOSED` 各环节按钮显隐均与 T1 2.2/2.3 一致。

## 4. 问题清单与修复记录

| # | 问题 | 发现方式 | 修复 | 状态 |
|---|---|---|---|---|
| 1 | 无（本次核查未发现新问题；历史问题已在 C3/C4 修复：Shop.vue 导入名、Orders.vue 地址拼装、CODE_MESSAGES 文案等） | - | - | ✅ 0 遗留 |

## 5. 待 M3 联调补充项（转入 C6）

- [ ] 真实 token 下验证 1002 过期跳登录、1003 角色拦截的实际后端返回
- [ ] seed.sql 起库后跑通四条链路真实流转（含审计日志留痕验证）
- [ ] A-013 订单查询如并入争议处理页，同步更新映射表第 2 节
