# W-07 真实联调记录（用户网页端）

> 成员 B（用户网页端） ｜ 日期：2026-08-29 ｜ 依赖：M3 达成（A：H2 MySQL 兼容模式，后端 8080，67 接口可调用）
> 说明：本交付物为「W-06 待 M3 联调验证清单」的落实记录——以真实后端 token + 真实数据逐接口验证，重点覆盖四类契约修复点与错误码分支。

## 1. 联调环境

| 项 | 值 |
|---|---|
| 后端 | `backend/target/jd-shop-1.0.0.jar --spring.profiles.active=h2`（A 交付物 M3，启动命令见 `docs/phase3/member-a/deliverables/M3-后端可联调交付物.md`） |
| 后端地址 | http://localhost:8080 |
| 本端 dev | `npm run dev` → http://localhost:5174（vite 代理 `/api` → 8080，配置见 `frontend-user/vite.config.js:19`） |
| 数据库 | H2 文件库 `./data/jd_shop`（重启持久化；重置基线 = 停服 → 删 `./data` → 重启自动重灌 schema+seed） |
| 验证账号 | user001/user002（USER）、merchant001/merchant002（MERCHANT）、admin001（ADMIN）；密码=账号名；短信验证码固定 `123456` |

验证方式：HTTP 层全量调用（Invoke-RestMethod），逐接口断言返回信封 code 与 data 结构，两轮共 47 个用例 + 快照记录见本文件；UI 层走查在 dev server 上点验（本轮完成路由/代理就绪验证，页面级 browser-use 走查见 M4 步骤）。

## 2. 公共组 P-001~P-008 验证

| 编号 | 验证点 | 结果 | 实测 |
|---|---|---|---|
| P-001 | 注册 `{username,password,phone}` | ✅ | 新注册 usrtest 成功，data.userId 返回（envelope data 为 `{userId}`） |
| P-002 | 登录 `{username,password}` | ✅ | user001/merchant001/admin001 均返回 `{token,userInfo}`；**错误密码 → 2003** 实测一致 |
| P-003 | 分类列表 | ✅ | 13 条返回，`data={list:[...],total}`；信封 total=null（与 W-06 记录一致） |
| P-004 | 商品分页 | ✅ | `?page=1&pageSize=5&categoryId=11` → total/list 正常 |
| P-005 | 商品详情 / 不存在 | ✅ | 101 正常；`/products/999999` → **3001** 实测一致 |
| P-006 | 商品评价分页 | ✅ | 101 评价列表 total 正常（空/有数据均可） |
| P-007 | 短信验证码 | ✅ | 发送成功 |
| P-008 | 短信登录 `/login/sms` | ✅ | 代码 `123456` 登录成功；**错误码 → 2003**；**2005 幽灵码未出现（与 W-06 判定一致，JSDoc 已修）** |

## 3. 用户组 U-001~U-025 验证（关键结论节选）

| 编号 | 验证点 | 结果 | 实测 |
|---|---|---|---|
| U-001 | 个人资料 | ✅ | 返回 username/role 等；无 token → **1002** 实测一致 |
| U-002 | 修改资料/改密 | ✅ | **字段 `oldPassword/newPassword` 实测正确**；旧密码错 → **2003**；改密后用新密码登录成功；`isDefault` 需整数语义见下 |
| U-003 | 地址列表 | ✅ | 初始实测裸数组（W-06 偏差）；**A 已修复为信封 `{list,total}`（fbe7b71）并复验**，B 端 API 双形态归一化消费，渲染正常 |
| U-004 | 新增地址 | ✅ | **`isDefault` 接受整数 0/1；传布尔 true → 请求体格式错（1001）**。B/D 端已按 0/1 下发（address.js 注释即为 0/1），无 bug |
| U-005 | 修改地址 | ✅ | 正常 |
| U-006 | 删除地址 | ✅ | 正常 |
| U-007 | 设默认 | ✅ | 正常 |
| U-008 | 购物车列表 | ✅ | 初始实测裸数组；**A 已修复为信封 `{list,total}`（772ddec）并复验**，B 端 API 双形态归一化消费，Cart/Checkout 渲染正常 |
| U-009 | 加购 | ✅ | 正常；**下架商品(113) → 3002；不存在商品(99999) → 3001** 实测一致 |
| U-010 | 修改数量/勾选 | ✅ | **`quantity/selected` 接受整数；传布尔 → 1001**。B/D 端按 0/1 下发（cart.vue `selected: target`），无 bug；非本人条目 → **4003** |
| U-011 | 删购物车项 | ✅ | 正常 |
| U-012 | 创建订单 | ✅ | **返回 `{orders:[{orderId,orderNo,status,payAmount}]}` 实测**（首字段为 `orderId` 而非 `id`）；空 cartItemIds → **1001「请勾选要结算的商品」**（参数校验先于业务码，王 W-06 预判的 4004 实际被 1001 拦截）；无效地址 → **4005**；已失效购物车项/非本人 → **4003** |
| U-013 | 订单列表 | ✅ | 6 Tab status 过滤正常（PAID/COMPLETED 等） |
| U-014 | 订单详情 | ✅ | orderNo/items 齐全；不存在 → **4001** 实测一致 |
| U-015 | 支付 | ✅ | `{paymentNo}` 实测（新订单闭环支付成功）；重复支付/价格变化（4008/4007）未在本轮真实触发（需多端并发时序，见 5） |
| U-016 | 取消订单 | ✅ | PENDING_PAY 取消成功；**PAID 取消 → 4002** 实测一致 |
| U-017 | 确认收货 | ✅ | SHIPPED→COMPLETED 成功（联动商家发货 M-011） |
| U-018 | 申请退款 | ✅ | 初始 `{refundId,status}` 无 refundNo；**A 已补齐 refundNo（772ddec）复验正常**；重复申请 **5003** 实测一致 |
| U-019 | 售后列表 | ✅ | status 过滤正常 |
| U-020 | 撤销退款 | ✅ | REFUNDING 撤销成功 |
| U-021 | 申请平台介入 | ✅ | **MERCHANT_REJECTED 状态介入 → ADMIN_INTERVENED 成功**（联动商家拒绝 M-014）；直接对 REFUNDING 介入 → **5005（状态不允许）** 实测一致（W-06 待验证项闭环） |
| U-022 | 通知列表 | ✅ | 分页/readStatus 正常 |
| U-023 | 通知已读 | ✅ | 已读成功；非本人 → **1004** 实测一致 |
| U-024 | 评价 | ✅ | 已完成订单评价成功（rating=5） |
| U-025 | 全部已读 | ✅ | 正常 |

## 4. 跨角色闭环验证（B 端用户域 + A/C 商家侧配合）

以新注册测试账号跑通真实链路：**加购 → 下单(U-012) → 支付(U-015) → 商家发货(M-011, merchant001) → 确认收货(U-017) → 申请退款(U-018) → 商家拒绝(M-014) → 申请介入(U-021) → 售后单进入 ADMIN_INTERVENED**，全 200 通过。该链路证明 B/D 端用户域 33 接口与后端实现完全对齐（含 W-06 全部 4 处契约偏差的消费方式）。

## 5. 联调中确证的契约细节校准（T5 v1.1 修订建议）

在 W-06 §4 偏差表基础上，真实联调补充确认 3 条：

1. **U-012 返回体首字段为 `orderId`**（T5 未注明字段名），且空购物车选择时由参数校验 `1001「请勾选要结算的商品」` 拦截，4004/空车业务分支非经此路径（记录为主，前端不受影响）。
2. **U-004/U-010/U-011 布尔字段 `isDefault`/`selected` 仅接受整数 0/1**——T5 数据字典建议明确「0=否/1=是」；前端已按 0/1 下发，无兼容问题。
3. **U-018 返回仅 `{refundId,status}`（初始实测无 refundNo）**——已由 A 在 `772ddec fix-U003-U008-U018-contract` 从后端补齐 `refundNo`，复验实测 `RF20260829185237000` 正常返回（见 §8）。

> ★ 契约偏差闭环说明：W-06 记录的两处返回形态偏差（U-003/U-008 裸数组、U-018 无 refundNo）已在复验中确认——A 分别以 `fbe7b71 fix-address-api-contract`（U-003 → `{list,total}`）与 `772ddec`（U-008 → `{list,total}`、U-018 → refundNo）从后端对齐 T5；B 端 API 层改为双形态归一化（数组或 `{list,total}` 均兼容，`frontend-user/src/api/{cart,address}.js`），页面消费无感，正反向均不再依赖特定形态。

## 8. 契约修复复核（2026-08-29 晚，A 后端修复后）

- 重建后端 jar（含 fbe7b71/772ddec 两个契约修复）并以 H2 干净库复验：
  - `U-003`：`data` = `{list,total}`（listCount=1）✅
  - `U-008`：`data` = `{list,total}`（listCount=2）✅
  - `U-018`：`data` = `{refundId,refundNo,status}`（refundNo=RF20260829185237000）✅
  - `U-012`：`orders[].orderId` 形态不变 ✅
- 本端适配：`frontend-user/src/api/cart.js`、`address.js` 的列表接口改为双形态归一化（`Array.isArray(d) ? d : (d && d.list) || []`），`npm run build` 通过。
- 说明：前端正常消费任意一种后端形态；A 的修复使后端完全对齐 T5 v1.1 信封约定，偏差清单对应项视为闭环。

## 6. 遗留（依赖 UI 走查 / 其他端）

- [ ] 13 页页面级 browser-use 走查 + 截图归档（M4 验收项，需在 dev server 5174 上执行；本轮完成环境就绪）
- [ ] U-015 4007（价格变化）/4008（重复支付）时序场景专用用例（需商家改价 + 双端并发）
- [ ] 多店拆单 `{orders:[...]}` 双商家渲染、通知徽标数量联动（真实数据）
- [ ] 截图归档后并入演示脚本（B 侧 5 账号闭环脚本见 X/B 演示脚本备料）

## 7. 验证脚本留痕

- 联调脚本：本地临时 `liaison.ps1` / `liaison2.ps1`（Invoke-RestMethod，输出 JSON 快照），供复现；两轮结果：Round1 39 过 / 13 失败均为脚本字段假设错误（`id` vs `orderId`、布尔 vs 整数），Round2 修正后 **26/26 全过**。
- 环境就绪验证：`GET http://localhost:5174/` → 200（挂载点存在）；`GET http://localhost:5174/api/categories` → 200 JSON（vite 代理联通后端）。