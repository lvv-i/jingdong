# W-05 个人中心链路与全链路自测（成员 B 交付物）

> 周次：W5（Phase 4 · Day 4-5）｜ 日期：2026-08-15 ｜ 状态：✅ 完成（真实全链路闭环待 M3）
> 对应任务书：`docs/phase4/member-b/tasks/W1-W5-用户网页端任务书.md` §W5

## 一、本周目标

个人中心 / 地址管理 / 消息通知接通 U-001~U-007、U-022~U-025；13 页自测与空状态收口。

## 二、完成内容

### 1. 个人中心（B-P11 `/profile`）

**文件**：`src/views/Profile.vue`（重写）、`src/api/auth.js`（复用 getProfile/updateProfile）

| 用例 | 说明 |
|---|---|
| U-001 个人资料展示 | 用户名 / 手机号 / 角色标签（USER/MERCHANT/ADMIN 映射）；加载失败显示 `-` 兜底 |
| U-002 修改手机号 | 前端校验 `^1[3-9]\d{9}$`；留空表示不修改；成功后同步 Pinia userStore 与 localStorage |
| U-002 修改密码 | 原密码必填 + 新密码 6-20 位 + 两次一致校验；成功后清空表单 |

- 契约对齐：`GET /users/profile` → {username, phone, role}；`PUT /users/profile` body 支持 {phone} / {oldPassword, newPassword}（ProfileUpdateDTO）。

### 2. 地址管理（B-P12 `/addresses`）

**文件**：`src/views/AddressList.vue`（重写）、`src/api/address.js`（补全 U-004~U-007）

| 用例 | 说明 |
|---|---|
| U-003 地址列表 | 卡片双列网格；默认地址红色 tag |
| U-004 新增地址 | 弹窗表单：收货人/手机号/省市区/详细地址/是否默认；满 20 个禁用新增按钮 |
| U-005 编辑地址 | 复用弹窗，editingId 区分新增/编辑 |
| U-006 删除地址 | ElMessageBox 二次确认 |
| U-007 设为默认 | 成功后刷新列表 |
| 2006 上限提示 | 捕获 err.code === 2006 →「收货地址已达上限（20 个），请先删除不需要的地址」 |

- 契约对齐：`GET /addresses`、`POST /addresses`、`PUT /addresses/{id}`、`DELETE /addresses/{id}`、`PUT /addresses/{id}/default`；手机号校验 `^1[3-9]\d{9}$`；isDefault 0/1。

### 3. 消息通知（B-P13 `/notifications`）

**文件**：`src/views/Notifications.vue`（重写）、`src/api/notification.js`（新建）

| 用例 | 说明 |
|---|---|
| U-022 通知列表 | 三 Tab 筛选（全部/未读/已读 → readStatus 空/0/1）+ 分页（page/pageSize） |
| U-023 单条已读 | 点击未读项 → `PUT /notifications/{id}/read`，本地置已读；未读 Tab 下自动刷新列表 |
| U-025 全部已读 | 头部按钮显示未读计数，无未读时禁用 → `PUT /notifications/read-all` |

- 契约对齐：NotificationVO {id, title, content, readStatus 0/1, createdAt}；未读项红点 + 高亮背景。

### 4. 13 页空状态与错误兜底核查（P1：无数据不空白）

| # | 页面 | 空状态方案 | 结果 |
|---|---|---|---|
| 1 | 首页 / | 类目 `el-empty`；商品 EmptyState「暂无商品」 | ✅ |
| 2 | 搜索 /search | EmptyState「没有找到相关商品」+ 清空条件按钮 | ✅ |
| 3 | 商品详情 /product/:id | 详情 EmptyState「暂无详情」；评价「暂无评价」 | ✅ |
| 4 | 登录 /login | 表单页（无需空状态） | ✅ |
| 5 | 购物车 /cart | EmptyState「购物车还是空的」+ 去逛逛 | ✅ |
| 6 | 结算 /checkout | 地址空 / 商品空双 EmptyState + 引导按钮 | ✅ |
| 7 | 订单中心 /orders | EmptyState「暂无相关订单」（按 Tab 文案） | ✅ |
| 8 | 订单详情 /orders/:id | notFound → el-result「订单不存在」（4001 兜底） | ✅ |
| 9 | 售后中心 /refunds | EmptyState「暂无售后记录」 | ✅ |
| 10 | 评价 /orders/:id/review | EmptyState「没有待评价的商品」 | ✅ |
| 11 | 个人中心 /profile | 表单页，加载失败字段 `-` 兜底 | ✅ |
| 12 | 地址管理 /addresses | EmptyState「还没有收货地址」+ 新增按钮 | ✅ |
| 13 | 消息通知 /notifications | EmptyState「暂无消息」 | ✅ |

所有列表页请求统一走 `silent: true` 模式：无后端/接口异常时静默降级为空状态，不弹错误、不空白。

### 5. browser-use 端到端实测

- 环境：vite dev（5173）+ 后端未启动（silent 降级模式）
- 实测路径（2026-08-15）：
  - `/profile` → 页面渲染正常，资料区加载态 → 空兜底
  - `/addresses` → 渲染正常，空状态 + 新增按钮
  - `/notifications` → 渲染正常，三 Tab + 空状态
- 控制台无 JS 报错；唯一 500 为 `GET /api/notifications`（Vite proxy 后端未启动所致，已被 silent 模式捕获）。
- 截图：`w5-profile.png`、`w5-addresses.png`、`w5-notifications.png`（本目录）

## 三、遗留与移交（依赖 M3）

1. **全链路 5 账号闭环自测**（D-02 方案：登录 → 浏览 → 加购 → 下单 → 支付 → 收货 → 评价 → 退款）：需后端 + MySQL 联调环境。本机无 MySQL（3306 未监听），M3 环境由 A 负责搭建；M3 达成后按 `docs/phase4/member-c/tasks/C4-C6-后台端任务书.md` 的演示脚本执行。
2. **与 A 联调记录归档**：接口版本号、问题清单、决议在 M3 联调时统一归档（A 侧 S1-S3 任务书已包含联调流程）。

## 四、验收对照

- [x] 个人中心 / 地址管理 / 消息通知三个页面完成，U-001~U-007、U-022~U-025 全接通
- [x] 13 页全部可访问，空状态/错误兜底无死角
- [x] browser-use 验证核心页面
- [x] `npm run build` 通过（5.08s）
- [ ] 全链路演示路径可复现 —— 待 M3（见遗留）
